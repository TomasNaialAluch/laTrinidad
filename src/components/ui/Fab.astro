---
---
<a class="fab" href="/order" aria-label="Hacer pedido">Hacer pedido</a>
<style>
  .fab {
    position: fixed;
    right: var(--space-md);
    bottom: var(--space-md);
    z-index: 50;
    padding: 0.9rem 1.4rem;
    border-radius: 999px;
    background: var(--color-brown);
    color: var(--color-white);
    font-weight: 700;
    text-decoration: none;
    box-shadow: 0 6px 18px rgb(0 0 0 / 0.3);
    transition: transform 0.2s;
  }

  .fab:hover {
    transform: scale(1.05);
  }
</style>
